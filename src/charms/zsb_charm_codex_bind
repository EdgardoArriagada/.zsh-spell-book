#!/usr/bin/env zsh
set -euo pipefail

local paneId=${1:?paneId required}
local sessionId
sessionId=$(zsb_charm_codex_session_id "$paneId") || exit 0

zsb_tmux_agent_notification --bind-codex "$sessionId" "$paneId"
