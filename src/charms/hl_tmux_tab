#!/usr/bin/env zsh
set -o errexit
set -o pipefail

if [[ -z "${TMUX:-}" ]]; then
  echo "This script must be run inside tmux" >&2
  exit 1
fi

if [[ "$1" = "--clear" ]]; then
  tmux clear1;
  tmux clear2;
else
  tmux alert1;
  tmux alert2;
fi

